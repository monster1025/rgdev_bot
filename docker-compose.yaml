version: '3'
services:
  rgdev:
    build: ./
    container_name: rgdev
    restart: always
    volumes:
      - "./sent.json:/app/sent.json" 
    environment:
      - TZ=Europe/Moscow
    logging:
      driver: json-file
      options:
        max-size: 50m 
